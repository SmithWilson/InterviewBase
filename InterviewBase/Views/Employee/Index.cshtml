@model List<InterviewBase.Models.Entities.Employee>

@{
    ViewBag.Title = "Сотрудники";
}

<div class="panel panel-default" style="margin-top:20px;">
    <!-- Default panel contents -->
    <div class="panel-heading">Клиенты</div>

    <!-- Table -->
    <table class="table">
        <thead>
            <tr>
                <th scope="col">Id</th>
                <th scope="col">Имя</th>
                <th scope="col">Фамилия</th>
                <th scope="col">Возраст</th>
                <th scope="col">Email</th>
                <th scope="col"></th>
                <th scope="col"></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var employee in Model)
            {
                <tr>
                    <th scope="row">@employee.Id</th>
                    <td>@employee.FirstName</td>
                    <td>@employee.LastName</td>
                    <td>@employee.Age</td>
                    <td>@employee.Email</td>
                    <th>@Html.ActionLink("Удалить", "Renove", "Employee", new { idemployee = employee.Id }, null)</th>
                    <th>@Html.ActionLink("Изменить", "Update", "Employee", new { idemployee = employee.Id }, null)</th>
                </tr>
            }
        </tbody>
    </table>
</div>

@Html.ActionLink("Добавить", "Add", "Employee")

<nav aria-label="Page navigation">
    <ul class="pagination">
        <li>
            @{
                var prev = ViewBag.Index - 1;
                prev = prev < 0 ? 0 : prev;
            }
            <a href="/Employee/Index?index=@prev" aria-label="Previous">
                <span aria-hidden="true">&laquo;</span>
            </a>
        </li>
        @{
            var count = ViewBag.Count / 10;
            if (count >= 5)
            {
                count = 5;
            }
        }
        @for (int i = 0; i <= count; i++)
        {
            var index = i + 1;
            <li><a href="/Employee/Index?index=@i">@index</a></li>
        }
        <li>
            @{
                var next = ViewBag.Index + 1;
                next = next > count ? count : next;
            }
            <a href="/Employee/Index?index=@next" aria-label="Next">
                <span aria-hidden="true">&raquo;</span>
            </a>
        </li>
    </ul>
</nav>